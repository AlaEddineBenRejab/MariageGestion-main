<form method="post" action="{{ path('app_centres_mariages_delete', {'id': centres_mariage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ centres_mariage.id) }}">
    <button class="btn btn-warning waves-effect waves-light">Delete</button>
</form>
