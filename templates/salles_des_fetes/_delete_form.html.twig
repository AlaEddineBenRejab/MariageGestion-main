<form method="post" action="{{ path('app_salles_des_fetes_delete', {'id': salles_des_fete.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salles_des_fete.id) }}">
    <button class="btn btn-warning waves-effect waves-light">Delete</button>
</form>
